{
  "name": "cookie-clicker-nodejs",
  "version": "1.0.0",
  "description": "Cookie clicker on Node.js realization",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron build/main.js",
    "build": "tsc && cp src/index.html build/index.html"
  },
  "author": "Rienel",
  "license": "ISC",
  "dependencies": {
    "electron": "^2.0.3",
    "typescript": "^3.0.1"
  }
}
